spring.application.name=TechTitans

# --- NEON POSTGRESQL CONNECTION ---
# 1. URL must NOT have the username/password or the @ symbol in it!
spring.datasource.url=jdbc:postgresql://ep-square-flower-aot74u6k-pooler.c-2.ap-southeast-1.aws.neon.tech/neondb?sslmode=require
# 2. Pass the credentials here instead:
spring.datasource.username=neondb_owner
spring.datasource.password=npg_WKxS4UXnf8DO
spring.datasource.driver-class-name=org.postgresql.Driver

# --- HIBERNATE SETTINGS ---
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

# --- PROXY MAZE PERFORMANCE ---
spring.jpa.properties.hibernate.jdbc.batch_size=100
spring.jpa.properties.hibernate.order_inserts=true
spring.datasource.hikari.maximum-pool-size=15

# --- JSON FORMATTING FIX (CRITICAL FOR GRADING) ---
spring.jackson.property-naming-strategy=SNAKE_CASE